import 'package:flutter/material.dart';

class ErrorZonesScreenOne extends StatelessWidget {
  const ErrorZonesScreenOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ErrorWidget('Test');
  }
}
